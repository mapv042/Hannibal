[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    llm: calls a real LLM through a running simulator (tests/evals); run explicitly with -m llm
addopts = -m "not llm"
